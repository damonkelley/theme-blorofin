[tools]
hugo = "0.91.2"

[tasks.serve]
description = "Start Hugo dev server with live reload"
run = "hugo server -s dev-site"

[tasks.build]
description = "Build the site to dev-site/public/"
run = "hugo -s dev-site"

[tasks.clean]
description = "Remove generated files"
run = "rm -rf dev-site/public dev-site/resources"
